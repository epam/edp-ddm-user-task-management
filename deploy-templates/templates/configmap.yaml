apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}
data:
  application.yml: |-
    bpms.url: http://bpms:8080
    dso.url: http://digital-signature-ops:8080
    ceph.http-endpoint: https://s3-openshift-storage.{{ .Values.dnsWildcard }}
    spring.zipkin.baseUrl: http://zipkin.istio-system:9411
    form-management-provider.url: http://form-management-provider:3001
    server.max-http-header-size: 16KB